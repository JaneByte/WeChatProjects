<view class="profile-page">
  <view class="profile-hero">
    <view class="user-card">
      <view wx:if="{{isLoggedIn}}" class="avatar-container" bindtap="onOpenProfileEdit">
        <image class="avatar" src="{{userInfo.avatar}}" mode="aspectFill" binderror="onAvatarError"></image>
      </view>
      <view wx:else class="avatar-container">
        <image class="avatar" src="{{userInfo.avatar}}" mode="aspectFill" binderror="onAvatarError"></image>
      </view>
      <view class="user-meta">
        <text class="nickname">{{userInfo.nickname}}</text>
        <text wx:if="{{currentUserIdText}}" class="profile-id-line">{{currentUserIdText}}</text>
        <view wx:if="{{profileIncomplete}}" class="profile-tip-row">
          <text class="profile-tip">头像和昵称还可以再完善</text>
          <text class="profile-tip-link" bindtap="onCompleteProfile">去完善</text>
        </view>
        <view class="meta-bottom-row">
          <button wx:if="{{!isLoggedIn}}" class="login-btn" size="mini" bindtap="onOpenLogin">去登录</button>
          <text wx:elif="{{loginReady}}" class="hero-refresh" bindtap="onSwitchUserId">刷新资料</text>
          <text wx:else class="hero-status">正在同步账号状态...</text>
        </view>
      </view>
    </view>
  </view>
  <view class="order-card empty-order-card" wx:if="{{loginPromptVisible}}">
    <text class="empty-order-text">登录后可同步订单、地址和优惠券，下单会更方便</text>
  </view>
  <view class="order-card">
    <view class="card-head">
      <text class="card-title">我的订单</text>
      <text class="card-link">常用订单入口都在这里</text>
    </view>
    <view class="order-grid">
      <view class="order-item {{activeOrderTab === item.key ? 'order-item--active' : ''}}" wx:for="{{orderTabs}}" wx:key="key" data-key="{{item.key}}" bindtap="onOrderTap">
        <view class="order-icon-box">
          <text class="order-mark">{{item.mark}}</text>
        </view>
        <text class="order-label">{{item.label}}</text>
      </view>
    </view>
  </view>
  <view class="order-card" wx:if="{{orderList.length > 0}}">
    <view class="card-head">
      <text class="card-title">最近订单</text>
      <text class="card-link">最近下单记录</text>
    </view>
    <view class="recent-order" wx:for="{{orderList}}" wx:key="id">
      <view class="recent-top">
        <text class="recent-no">#{{item.orderNo}}</text>
        <text class="recent-status">{{item.statusText}}</text>
      </view>
      <view class="recent-mid">
        <text class="recent-amount">实付 ¥{{item.actualAmount}}</text>
        <text class="recent-count">{{item.itemCount}}件商品</text>
      </view>
      <view class="recent-actions">
        <button wx:if="{{item.status === 0}}" class="action-btn action-btn--primary" size="mini" disabled="{{submittingOrderAction}}" data-id="{{item.id}}" bindtap="onPayOrder">去支付</button>
        <button wx:if="{{item.status === 0 || item.status === 1}}" class="action-btn" size="mini" disabled="{{submittingOrderAction}}" data-id="{{item.id}}" bindtap="onCancelOrder">取消订单</button>
        <button wx:if="{{item.status === 2}}" class="action-btn" size="mini" disabled="{{submittingOrderAction}}" data-id="{{item.id}}" bindtap="onFinishOrder">确认收货</button>
      </view>
    </view>
  </view>
  <view class="order-card empty-order-card" wx:elif="{{!loginPromptVisible}}">
    <text class="empty-order-text">还没有订单记录，先去逛逛今天的新鲜好物</text>
  </view>
  <view class="menu-card">
    <view class="card-head card-head--menu">
      <text class="card-title">账号服务</text>
      <text class="card-link">资料、收货地址和常用服务</text>
    </view>
    <view class="menu-item" wx:for="{{menuList}}" wx:key="key" data-key="{{item.key}}" bindtap="onMenuTap">
      <view class="menu-main">
        <text class="menu-title">{{item.label}}</text>
        <text class="menu-desc">{{item.desc}}</text>
      </view>
      <text class="menu-arrow">></text>
    </view>
  </view>
</view>
